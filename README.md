## Setup

1. Create all pages
2. Set Settings->Reading to use static pages
3. Appearance->Menus Make menu with submenus. Enable Display location primary
    1. Fold out the top "Screen options" menu, enable custom CSS
    2. Add the `donate-menu-item` css class to the "Doneer nu" button
4. Settings->Permalink postname